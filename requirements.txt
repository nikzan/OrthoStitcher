opencv-python>=4.x.x  # Используйте актуальную версию, например, opencv-python==4.8.1.78
numpy>=1.x.x                 # Используйте актуальную версию, например, numpy==1.26.4
tqdm>=4.x.x                   # Используйте актуальную версию, например, tqdm==4.66.1